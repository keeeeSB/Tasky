%h2.text-center.mb-3 タスク詳細

.row.justify-content-center
  .col-md-6.text-center
    .card.shadow-sm.mb-3
      .card-body.py-3
        .card-title.fw-bold= @task.title
        %p.mb-3= h(@task.description)
        = button_to @task.completed? ? '完了を取り消す' : '完了にする',
                    toggle_users_team_task_path(@task),
                    method: :patch,
                    class: "btn btn-sm #{@task.completed? ? 'btn-secondary' : 'btn-outline-success'}"

    .mb-3.d-flex.justify-content-center.gap-3
      = link_to '一覧に戻る', users_team_path, class: 'btn btn-outline-secondary'
      = link_to '編集する', edit_users_team_task_path(@task), class: 'btn btn-outline-warning'
      = button_to '削除する',
                  users_team_task_path(@task),
                  method: :delete,
                  data: { turbo_confirm: '本当に削除しますか？' },
                  form: { class: 'd-inline' },
                  class: 'btn btn-outline-danger'
