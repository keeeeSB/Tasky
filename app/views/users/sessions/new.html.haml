%h2.text-center.mb-3 ログイン

.row.justify-content-center
  .col-md-6
    = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .form-inputs
        = f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: 'email' }
        = f.input :password,
                  required: false,
                  input_html: { autocomplete: 'current-password' }
        - if devise_mapping.rememberable?
          = f.input :remember_me, as: :boolean

      .form-actions.text-center
        = f.button :submit, 'ログインする', class: 'btn btn-outline-primary'

    .text-center.mt-4
      %div
        = link_to 'パスワードをお忘れですか？', new_user_password_path, class: 'text-muted'
      %p.my-3 または
      %div
        = link_to 'アカウント確認メールを再送信', new_user_confirmation_path, class: 'text-muted'
