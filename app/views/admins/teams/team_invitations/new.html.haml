%h2.text-center.mb-3 チーム招待

.row.justify-content-center
  .col-md-6
    = simple_form_for([:admins, @team, @invitation]) do |f|
      .form-inputs
        = f.input :email, autofocus: true

      .form-actions.text-center
        = f.button :submit, '招待する', class: 'btn btn-outline-primary'
